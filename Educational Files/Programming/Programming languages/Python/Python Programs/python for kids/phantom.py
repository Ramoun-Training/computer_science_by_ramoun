import turtle
import time
phantom=turtle
def square (x6,black,white,fanta):
    for four in range(0,fanta):
            if four%2==0:
                phantom.left(black)
            else:
                phantom.left(white)
            phantom.forward(x6)
		
#x6:controls the size of the shape
#x5:controls the angle thus controls the shape
#fanta:controls the irertor thus controls the final shape
    
